{
  "name": "@loggy/client",
  "version": "1.0.0",
  "description": "",
  "main": "src/index.ts",
  "scripts": {
    "start": "esbuild --watch ./src/index.tsx --bundle --loader:.svg=text --loader:.css=local-css --outdir=build",
    "build": "esbuild --minify ./src/index.tsx --bundle --loader:.svg=text --loader:.css=local-css --outdir=build"
  },
  "keywords": [],
  "author": "Teemu Pääkkönen",
  "license": "UNLICENSED",
  "packageManager": "pnpm@10.25.0",
  "dependencies": {
    "@loggy/common": "link:../common",
    "@material-design-icons/svg": "^0.14.15",
    "blurhash": "^2.0.5",
    "esbuild": "^0.27.1",
    "kaiku": "^1.26.1"
  }
}
